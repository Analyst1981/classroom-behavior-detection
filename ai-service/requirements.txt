# ===== 基础运行（必需） =====
flask>=2.3
flask-cors>=4.0
flask-socketio>=5.3
opencv-python-headless>=4.8
numpy>=1.24
pillow>=10.0
requests>=2.28
python-dotenv>=1.0
reportlab>=4.0

# ===== 真实 YOLO 推理（可选，缺失时自动降级为演示引擎） =====
# Windows CPU 版建议先执行：
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# 再执行：
#   pip install ultralytics
# 直接 pip install ultralytics 会拉取带 CUDA 依赖的默认 wheel（体积约 2GB+）
torch>=2.1
torchvision>=0.16
ultralytics>=8.1
